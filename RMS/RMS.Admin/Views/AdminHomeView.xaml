<UserControl x:Class="RMS.Admin.Views.AdminHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:Views="clr-namespace:RMS.Admin.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600">
    <Grid>
        <TabControl SelectedIndex="{Binding TabSelectedIndex}">
            <TabItem Header="User">
                <Views:UserView DataContext="{Binding UserViewModel}"/>
            </TabItem>
            <TabItem Header="Tab1" />
            <TabItem Header="Tab1" />
        </TabControl>
    </Grid>
</UserControl>
